# ===========================================================================
# requirements.txt - deepvision-cifar10-classifier
# Versions pinnées pour reproductibilité (Phase 0 baseline)
# Python : 3.12
# Plateforme cible : Windows 10/11 + Linux (x86_64) — CPU only
# ---------------------------------------------------------------------------
# Phase 1 introduira pyproject.toml + pip-tools pour figer les transitives.
# Pour les outils dev (pytest, ruff, mlflow, fastapi, ...), voir
# requirements-dev.txt (ajouté lors de la Phase 1).
# ===========================================================================

# Core ML
tensorflow-cpu==2.21.0
keras==3.14.0

# Numérique & scientifique
numpy==2.4.4
scikit-learn==1.8.0

# Imagerie
Pillow==12.2.0

# UI démonstration
streamlit==1.57.0

# Diagnostic local
psutil==7.2.2
