# ===========================================================================
# requirements.txt - deepvision-cifar10-classifier
# Versions pinnees pour reproductibilite (Phase 1 packaging)
# Python : 3.12
# Plateforme cible : Windows 10/11 + Linux (x86_64) - CPU only
# ---------------------------------------------------------------------------
# Pour les outils dev (pytest, ruff, mypy, pre-commit, bandit, pip-audit),
# voir requirements-dev.txt.
# Phase 5 introduira pip-tools pour figer aussi les transitives (lock-file).
# ===========================================================================

# --- Core ML ---------------------------------------------------------------
tensorflow-cpu==2.21.0
keras==3.14.1

# --- Numerique & scientifique ---------------------------------------------
numpy==2.4.4
scikit-learn==1.8.0
scipy>=1.13,<2.0  # used in deepvision.evaluation.calibration.fit_temperature

# --- Imagerie -------------------------------------------------------------
Pillow==12.2.0

# --- UI demonstration -----------------------------------------------------
streamlit==1.57.0

# --- CLI & configuration --------------------------------------------------
typer==0.25.1
pydantic-settings==2.14.1

# --- MLOps : tracking, registry, model serving ----------------------------
mlflow>=2.16,<3.0

# --- Serving REST API (Phase 5) -------------------------------------------
fastapi>=0.115,<1.0
uvicorn[standard]>=0.47.0,<1.0
python-multipart>=0.0.29,<1.0   # required by FastAPI for multipart/form-data uploads
prometheus-client>=0.21,<1.0

# --- Diagnostic local -----------------------------------------------------
psutil==7.2.2

# --- Export multi-runtime (Phase 10) --------------------------------------
# tf2onnx converts a Keras / SavedModel graph into an ONNX file.
# onnx is the format spec; onnxruntime is the CPU inference engine used by
# the latency benchmark and by external consumers of the exported artefact.
# Upper bounds intentionally loose (<2.0 / <3.0) so dependabot can ship
# minor bumps without breaking the export pipeline.
tf2onnx>=1.16,<2.0
onnx>=1.16,<3.0
onnxruntime>=1.18,<3.0
