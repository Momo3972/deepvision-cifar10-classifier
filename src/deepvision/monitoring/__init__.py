"""
Production monitoring and drift detection.

Will be populated in Phase 8 with:

- :mod:`deepvision.monitoring.drift`: image embedding drift (KL divergence /
  Wasserstein on the penultimate-layer features).
- :mod:`deepvision.monitoring.ood`:   out-of-distribution detection
  (energy score / Mahalanobis distance).
"""
