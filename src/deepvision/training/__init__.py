"""
Training pipeline.

Will be populated in Phase 3 with:

- :mod:`deepvision.training.train`:        main training entrypoint with MLflow autolog.
- :mod:`deepvision.training.callbacks`:    EarlyStopping, ReduceLROnPlateau, custom hooks.
- :mod:`deepvision.training.mlflow_utils`: MLflow logging helpers and registry promotion.
"""
