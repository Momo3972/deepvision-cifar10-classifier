# ============================================================================
# requirements-dev.txt - deepvision-cifar10-classifier
# Outils de developpement uniquement (NE PAS installer dans l'image de prod)
# Installer apres le requirements.txt principal.
#
# Strategie : versions exactes pour les outils critiques (ruff, mypy)
# qui peuvent introduire des breaking changes mineurs ; plages bornees
# pour le reste afin de tolerer les patches.
# ============================================================================

# --- Lint & format ----------------------------------------------------------
ruff==0.15.13

# --- Static type checking ---------------------------------------------------
mypy==1.18.2
types-requests==2.33.0.20260513

# --- Tests & coverage -------------------------------------------------------
pytest>=8.4,<10.0
pytest-cov>=6.0,<8.0
pytest-xdist>=3.6,<4.0
hypothesis>=6.100,<7.0
httpx>=0.27,<1.0          # FastAPI TestClient backend (Phase 5 API tests)

# --- Pre-commit -------------------------------------------------------------
pre-commit>=4.0,<5.0

# --- Security ---------------------------------------------------------------
bandit>=1.7,<2.0
pip-audit>=2.7,<3.0

# --- Documentation (Phase 11) -----------------------------------------------
# Material theme + static i18n for the bilingual EN/FR site, mkdocstrings
# for auto-generated API reference from NumPy-style docstrings, plus the
# helper plugins that keep the navigation in sync with the docs/ folder.
# Upper bounds left loose so Dependabot can ship minor bumps without
# breaking the docs build.
mkdocs-material>=9.5,<10.0
mkdocs-static-i18n>=1.2,<2.0
mkdocstrings[python]>=0.26,<1.0
mkdocs-gen-files>=0.5,<1.0
mkdocs-literate-nav>=0.6,<1.0
mkdocs-section-index>=0.3,<1.0
pymdown-extensions>=10.7,<11.0
